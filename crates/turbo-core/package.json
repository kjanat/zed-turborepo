{
  "name": "@turbo/core",
  "version": "2.8.0",
  "private": true,
  "scripts": {
    "build": "cargo build -p turbo-core",
    "build:release": "cargo build -p turbo-core --release",
    "check": "cargo check -p turbo-core",
    "test": "cargo test -p turbo-core",
    "lint": "cargo clippy -p turbo-core",
    "fmt": "cargo fmt -p turbo-core",
    "clean": "cargo clean -p turbo-core"
  }
}
