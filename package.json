{
  "name": "zed-turborepo",
  "private": true,
  "packageManager": "bun@1.3.8",
  "workspaces": ["crates/*", "!vendor"],
  "scripts": {
    "build": "turbo run build",
    "build:release": "turbo run build:release",
    "check": "turbo run check",
    "test": "turbo run test",
    "lint": "turbo run lint",
    "fmt": "turbo run fmt",
    "clean": "turbo run clean"
  },
  "devDependencies": {
    "@types/bun": "^1.3.8",
    "turbo": "^2.8.1"
  }
}
